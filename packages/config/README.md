# @kafka-playground/config

Shared package for env loading and validation.

Planned responsibilities:

- common env names.
- validation schemas.
- service-specific config factories.
- typed access to Kafka, Postgres, Redis and OpenTelemetry settings.
